name: pocket-js-server

services:
  pg:
    image: bitnami/postgresql:16.4.0
    ports:
      - '5433:5432'
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=inorbit
